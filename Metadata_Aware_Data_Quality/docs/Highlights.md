# Highlights

(3–5 bullets, evidence-based, typically max ~85 characters each per common journal limits — adjust to target journal's exact limit)

- Proposes a three-layer control model separating schema, contract, and semantic/provenance validation.
- Controlled 400-case benchmark: schema-only detection reaches 30% recall (F1 0.462).
- Executable contract checks raise recall to 70% (F1 0.824) on the same benchmark.
- Metadata-aware checks close all ten predefined violation classes with zero false positives.
- Framework is deterministic and lightweight; requires no machine-learning model.

*Note: verify exact character/word limit and required bullet count against the target journal's current Guide for Authors before submission — these vary by publisher.*
